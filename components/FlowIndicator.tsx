'use client';

import { useEffect, useState } from 'react';
import { useFlowStore } from '@/lib/store';
import { Circle, Square } from 'lucide-react';

export default function FlowIndicator() {
  // Component disabled to avoid obstructing other buttons
  return null;
}
